{
  "name": "server",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "bun run --hot src/index.ts",
    "start": "bun run dist/index.mjs"
  },
  "dependencies": {
    "hono": "catalog:",
    "@orpc/server": "catalog:",
    "@orpc/openapi": "catalog:",
    "@orpc/zod": "catalog:",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "zod": "catalog:",
    "socket.io": "^4.8.1",
    "@crossway/api": "workspace:*",
    "@crossway/auth": "workspace:*",
    "@crossway/db": "workspace:*",
    "@crossway/socket": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "catalog:",
    "@types/node": "^22.13.13",
    "typescript": "catalog:",
    "tsdown": "^0.16.5",
    "@crossway/config": "workspace:*"
  }
}
